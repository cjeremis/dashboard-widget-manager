/*!
 * Dashboard Widget Manager - Tools Bundle
 *
 * JavaScript bundle for the Tools admin page.
 *
 * @package    Dashboard_Widget_Manager
 * @version    1.0.0
 * @copyright  2026 TopDevAmerica
 * @license    GPL-2.0+
 */
/******/ (function() { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./assets/js/modules/modals/reset-data.js":
/*!************************************************!*\
  !*** ./assets/js/modules/modals/reset-data.js ***!
  \************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initResetModal: function() { return /* binding */ initResetModal; }
/* harmony export */ });
/* harmony import */ var _partials_dialog_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../partials/dialog.js */ "./assets/js/modules/partials/dialog.js");
/**
 * Dashboard Widget Manager - Reset Data Modal
 *
 * Handles the reset data modal, including export-before-reset and confirmed data deletion.
 *
 * @package Dashboard_Widget_Manager
 * @since 1.0.0
 */


const $ = jQuery;
function initResetModal() {
  $(document).on('click', '#dwm-open-reset-modal', function () {
    openModal('dwm-reset-data-modal');
  });

  // Export first button in reset modal — open the export options modal on top
  $(document).on('click', '#dwm-reset-export-first', function () {
    closeModal('dwm-reset-data-modal');
    openModal('dwm-export-options-modal');
  });

  // Reset confirm button
  $(document).on('click', '#dwm-reset-confirm-button', function () {
    const resetWidgets = $('#dwm-reset-widgets').is(':checked');
    const resetSettings = $('#dwm-reset-settings-check').is(':checked');
    const resetNotifications = $('#dwm-reset-notifications').is(':checked');
    if (!resetWidgets && !resetSettings && !resetNotifications) {
      window.DWMToast.warning('Please select at least one data type to reset.', {
        title: 'Reset Data'
      });
      return;
    }
    const $btn = $(this);
    (0,_partials_dialog_js__WEBPACK_IMPORTED_MODULE_0__.dwmConfirm)({
      title: 'Reset Data',
      message: 'Are you sure you want to permanently delete the selected data? This cannot be undone.',
      icon: 'trash',
      confirmText: 'Reset Data',
      onConfirm() {
        showLoading($btn);
        ajax('dwm_reset_data', {
          reset: {
            widgets: resetWidgets ? 1 : 0,
            settings: resetSettings ? 1 : 0,
            notifications: resetNotifications ? 1 : 0
          }
        }, function (data) {
          hideLoading($btn);
          closeModal();
          window.DWMToast.success(data.message || 'Data reset successfully.', {
            title: 'Reset Data'
          });
          setTimeout(function () {
            location.reload();
          }, 1500);
        }, function (data) {
          hideLoading($btn);
          window.DWMToast.error(data.message || 'Failed to reset data.', {
            title: 'Reset Data'
          });
        });
      }
    });
  });
}

/***/ }),

/***/ "./assets/js/modules/partials/cache-tools.js":
/*!***************************************************!*\
  !*** ./assets/js/modules/partials/cache-tools.js ***!
  \***************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initCacheTools: function() { return /* binding */ initCacheTools; }
/* harmony export */ });
/**
 * Dashboard Widget Manager - Cache Tools
 *
 * Handles clear cache tools actions and confirmation modal.
 *
 * @package Dashboard_Widget_Manager
 * @since 1.0.0
 */

const $ = jQuery;
function initCacheTools() {
  $(document).on('click', '#dwm-open-clear-caches-modal', function () {
    openModal('dwm-clear-caches-modal');
  });
  $(document).on('click', '#dwm-clear-caches-confirm', function () {
    const $btn = $(this);
    showLoading($btn);
    ajax('dwm_clear_caches', {}, function (data) {
      hideLoading($btn);
      clearClientDwmCaches();
      closeModal('dwm-clear-caches-modal');
      window.DWMToast.success(data.message || 'All caches have been cleared.', {
        title: 'Clear Caches'
      });
    }, function (data) {
      hideLoading($btn);
      window.DWMToast.error(data.message || 'Failed to clear caches.', {
        title: 'Clear Caches'
      });
    });
  });
}

/**
 * Remove browser-side cache artifacts used by DWM builders/wizard.
 */
function clearClientDwmCaches() {
  clearStorageByPrefix(sessionStorage, 'dwm_');
  clearStorageByPrefix(localStorage, 'dwm_');
}
function clearStorageByPrefix(storage, prefix) {
  if (!storage) {
    return;
  }
  const keysToDelete = [];
  for (let i = 0; i < storage.length; i++) {
    const key = storage.key(i);
    if (key && key.indexOf(prefix) === 0) {
      keysToDelete.push(key);
    }
  }
  keysToDelete.forEach(function (key) {
    storage.removeItem(key);
  });
}

/***/ }),

/***/ "./assets/js/modules/partials/demo-data.js":
/*!*************************************************!*\
  !*** ./assets/js/modules/partials/demo-data.js ***!
  \*************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initDemoData: function() { return /* binding */ initDemoData; }
/* harmony export */ });
/**
 * Dashboard Widget Manager - Demo Data
 *
 * Handles demo data import and deletion.
 *
 * @package Dashboard_Widget_Manager
 * @since 1.0.0
 */

const $ = jQuery;
function initDemoData() {
  // Open import demo modal
  $(document).on('click', '#dwm-open-import-demo-modal', function () {
    openModal('dwm-import-demo-modal');
  });

  // Confirm import demo
  $(document).on('click', '#dwm-import-demo-confirm', function () {
    const $btn = $(this);
    const importWidgets = $('#dwm-import-demo-widgets').is(':checked');
    const importNotifications = $('#dwm-import-demo-notifications').is(':checked');
    if (!importWidgets && !importNotifications) {
      window.DWMToast.warning('Please select at least one data type to import.', {
        title: 'Import Demo Data'
      });
      return;
    }
    showLoading($btn);
    ajax('dwm_import_demo_data', {
      import_widgets: importWidgets ? 1 : 0,
      import_notifications: importNotifications ? 1 : 0
    }, function (data) {
      hideLoading($btn);
      closeModal();
      window.DWMToast.success(data.message || 'Demo data imported successfully.', {
        title: 'Import Demo Data'
      });
      setTimeout(function () {
        location.reload();
      }, 1500);
    }, function (data) {
      hideLoading($btn);
      window.DWMToast.error(data.message || 'Failed to import demo data.', {
        title: 'Import Demo Data'
      });
    });
  });

  // Delete demo data
  $(document).on('click', '#dwm-delete-demo-data', function () {
    openModal('dwm-delete-demo-modal');
  });
  $(document).on('click', '#dwm-delete-demo-confirm', function () {
    const $btn = $(this);
    showLoading($btn);
    ajax('dwm_delete_demo_data', {}, function (data) {
      hideLoading($btn);
      closeModal('dwm-delete-demo-modal');
      window.DWMToast.success(data.message || 'Demo widgets deleted.', {
        title: 'Delete Demo Data'
      });
      setTimeout(function () {
        location.reload();
      }, 1500);
    }, function (data) {
      hideLoading($btn);
      window.DWMToast.error(data.message || 'Failed to delete demo widgets.', {
        title: 'Delete Demo Data'
      });
    });
  });
}

/***/ }),

/***/ "./assets/js/modules/partials/dialog.js":
/*!**********************************************!*\
  !*** ./assets/js/modules/partials/dialog.js ***!
  \**********************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   dwmConfirm: function() { return /* binding */ dwmConfirm; }
/* harmony export */ });
/**
 * Dashboard Widget Manager - Dialog Utility Module
 *
 * Provides a reusable, accessible confirm dialog that replaces native browser
 * confirm() and alert() calls with styled in-page modals.
 *
 * @package Dashboard_Widget_Manager
 * @since 1.0.0
 */

const MODAL_ID = 'dwm-dialog-modal';
function ensureModal() {
  if (document.getElementById(MODAL_ID)) {
    return;
  }
  const html = `
<div id="${MODAL_ID}" class="dwm-modal dwm-modal-sm">
	<div class="dwm-modal-overlay" id="dwm-dialog-overlay"></div>
	<div class="dwm-modal-content">
		<div class="dwm-modal-header">
			<h2>
				<span class="dashicons" id="dwm-dialog-icon"></span>
				<span id="dwm-dialog-title"></span>
			</h2>
			<button type="button" class="dwm-modal-close" id="dwm-dialog-close" aria-label="Close modal">&times;</button>
		</div>
		<div class="dwm-modal-body">
			<p id="dwm-dialog-message"></p>
		</div>
		<div class="dwm-modal-footer">
			<button type="button" id="dwm-dialog-cancel" class="dwm-button dwm-button-secondary">Cancel</button>
			<button type="button" id="dwm-dialog-confirm" class="dwm-button dwm-button-danger">Confirm</button>
		</div>
	</div>
</div>`;
  document.body.insertAdjacentHTML('beforeend', html);
}
function close() {
  const $modal = jQuery('#' + MODAL_ID);
  $modal.removeClass('active');
  if (jQuery('.dwm-modal.active').length === 0) {
    jQuery('body').removeClass('dwm-modal-open');
  }

  // Unbind one-time listeners
  jQuery('#dwm-dialog-confirm, #dwm-dialog-cancel, #dwm-dialog-close, #dwm-dialog-overlay').off('.dwmdialog');
}

/**
 * Show a confirm dialog.
 *
 * @param {Object} options
 * @param {string} options.title        Modal title.
 * @param {string} options.message      Body message.
 * @param {string} [options.icon]       Dashicons class suffix (default: 'warning').
 * @param {string} [options.confirmText] Confirm button label (default: 'Confirm').
 * @param {string} [options.confirmClass] Button class (default: 'dwm-button-danger').
 * @param {Function} options.onConfirm  Called when user confirms.
 * @param {Function} [options.onCancel] Called when user cancels (optional).
 */
function dwmConfirm({
  title,
  message,
  icon = 'warning',
  confirmText = 'Confirm',
  confirmClass = 'dwm-button-danger',
  onConfirm,
  onCancel = null
}) {
  ensureModal();
  const $ = jQuery;
  $('#dwm-dialog-title').text(title);
  $('#dwm-dialog-message').text(message);
  $('#dwm-dialog-icon').attr('class', 'dashicons dashicons-' + icon);
  $('#dwm-dialog-confirm').text(confirmText).attr('class', 'dwm-button ' + confirmClass);

  // Show modal
  $('#' + MODAL_ID).addClass('active');
  $('body').addClass('dwm-modal-open');

  // One-time event bindings
  $('#dwm-dialog-confirm').one('click.dwmdialog', function () {
    close();
    if (typeof onConfirm === 'function') {
      onConfirm();
    }
  });
  function cancel() {
    close();
    if (typeof onCancel === 'function') {
      onCancel();
    }
  }
  $('#dwm-dialog-cancel').one('click.dwmdialog', cancel);
  $('#dwm-dialog-close').one('click.dwmdialog', cancel);
  $('#dwm-dialog-overlay').one('click.dwmdialog', cancel);
}

/***/ }),

/***/ "./assets/js/modules/partials/export-import.js":
/*!*****************************************************!*\
  !*** ./assets/js/modules/partials/export-import.js ***!
  \*****************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initExportImport: function() { return /* binding */ initExportImport; }
/* harmony export */ });
/* harmony import */ var _dialog_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./dialog.js */ "./assets/js/modules/partials/dialog.js");
/**
 * Dashboard Widget Manager - Export / Import
 *
 * Handles widget and settings data export (download, view, copy) and JSON file import.
 *
 * @package Dashboard_Widget_Manager
 * @since 1.0.0
 */


const $ = jQuery;
let exportedData = null;
let selectedFile = null;
function initExportImport() {
  // Open export options modal
  $(document).on('click', '#dwm-export-button, #dwm-demo-export-button', function () {
    openModal('dwm-export-options-modal');
  });

  // Download from export options modal
  $(document).on('click', '#dwm-export-download-btn', function () {
    const options = getExportOptions();
    exportWithOptions(options, function (data) {
      const didDownload = download(JSON.stringify(filterData(data, options), null, 2), 'Export Data');
      if (didDownload) {
        closeModal('dwm-export-options-modal');
      }
    });
  });

  // Open view data modal
  $(document).on('click', '#dwm-view-data, #dwm-demo-view-data', function () {
    viewData();
  });

  // View data toggles
  $(document).on('change', '.dwm-view-toggle', function () {
    if (exportedData) {
      updateViewDataDisplay(exportedData);
    }
  });

  // Copy to clipboard
  $(document).on('click', '#dwm-modal-copy-json', function () {
    const text = $('#dwm-export-json-preview').val();
    copyToClipboard(text);
  });

  // Download from view modal
  $(document).on('click', '#dwm-modal-download-json', function () {
    const text = $('#dwm-export-json-preview').val();
    download(text, 'View Data');
  });

  // File selection
  $(document).on('change', '#dwm-import-file', function (e) {
    handleFileSelect(e);
  });

  // Drag and drop
  $(document).on('dragover', '#dwm-file-input-wrapper', function (e) {
    e.preventDefault();
    $(this).addClass('dwm-drag-over');
  });
  $(document).on('dragleave drop', '#dwm-file-input-wrapper', function (e) {
    $(this).removeClass('dwm-drag-over');
    if (e.type === 'drop') {
      e.preventDefault();
      const file = e.originalEvent.dataTransfer.files[0];
      if (file) {
        handleFileSelect({
          target: {
            files: [file]
          }
        });
      }
    }
  });

  // Remove file
  $(document).on('click', '#dwm-file-remove', function () {
    removeFile();
  });

  // Import button
  $(document).on('click', '#dwm-import-button', function () {
    importData();
  });
}
function getExportOptions() {
  return {
    widgets: $('#dwm-export-widgets').is(':checked'),
    settings: $('#dwm-export-settings').is(':checked'),
    notifications: $('#dwm-export-notifications').is(':checked')
  };
}
function exportWithOptions(options, callback) {
  ajax('dwm_export', {}, function (data) {
    callback(data);
  }, function (data) {
    window.DWMToast.error(data.message || 'Failed to export data.', {
      title: 'Export Data'
    });
  });
}
function filterData(data, options) {
  const filtered = {
    plugin: data.plugin,
    version: data.version,
    exported_at: data.exported_at,
    site_url: data.site_url
  };
  if (options.widgets && data.widgets) {
    filtered.widgets = data.widgets;
  }
  if (options.settings && data.settings) {
    filtered.settings = data.settings;
  }
  if (options.notifications && data.notifications) {
    filtered.notifications = data.notifications;
  }
  return filtered;
}
function download(jsonString, title = 'Export Data') {
  const normalized = (jsonString || '').trim();
  if (!normalized) {
    window.DWMToast.warning('No data available to download.', {
      title
    });
    return false;
  }
  try {
    JSON.parse(normalized);
  } catch (err) {
    window.DWMToast.warning('Export data is still loading or invalid.', {
      title
    });
    return false;
  }
  try {
    const blob = new Blob([normalized], {
      type: 'application/json'
    });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    const now = new Date().toISOString().replace(/[:.]/g, '-').slice(0, 19);
    a.href = url;
    a.download = 'dwm-export-' + now + '.json';
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
    window.DWMToast.success('JSON download started.', {
      title
    });
    return true;
  } catch (err) {
    window.DWMToast.error('Failed to process JSON download.', {
      title
    });
    return false;
  }
}
function setViewDataReady(ready) {
  $('#dwm-view-toggle-widgets, #dwm-view-toggle-settings, #dwm-view-toggle-notifications').prop('disabled', !ready);
  $('#dwm-modal-copy-json, #dwm-modal-download-json').prop('disabled', !ready);
}
function viewData() {
  openModal('dwm-view-data-modal');
  $('#dwm-export-json-preview').val('Loading export data...');
  setViewDataReady(false);
  ajax('dwm_export', {}, function (data) {
    exportedData = data;
    updateViewDataDisplay(data);
    setViewDataReady(true);
  }, function (data) {
    const message = data && data.message ? data.message : 'Failed to load data.';
    $('#dwm-export-json-preview').val(message);
    window.DWMToast.error(message, {
      title: 'View Data'
    });
  });
}
function updateViewDataDisplay(data) {
  const options = getViewToggles();
  const filtered = filterData(data, options);
  $('#dwm-export-json-preview').val(JSON.stringify(filtered, null, 2));
}
function getViewToggles() {
  return {
    widgets: $('#dwm-view-toggle-widgets').is(':checked'),
    settings: $('#dwm-view-toggle-settings').is(':checked'),
    notifications: $('#dwm-view-toggle-notifications').is(':checked')
  };
}
function copyToClipboard(text) {
  const $btn = $('#dwm-modal-copy-json');
  $btn.prop('disabled', true);
  const onClose = () => $btn.prop('disabled', false);
  if (navigator.clipboard && window.isSecureContext) {
    navigator.clipboard.writeText(text).then(function () {
      window.DWMToast.success('Copied to clipboard.', {
        onClose
      });
    });
  } else {
    const $temp = $('<textarea>').val(text).appendTo('body').select();
    document.execCommand('copy');
    $temp.remove();
    window.DWMToast.success('Copied to clipboard.', {
      onClose
    });
  }
}
function handleFileSelect(e) {
  const file = e.target.files && e.target.files[0];
  if (!file) return;
  selectedFile = file;
  const maxSize = 5 * 1024 * 1024;
  if (file.size > maxSize) {
    window.DWMToast.error('File size exceeds 5MB limit.', {
      title: 'Import Data'
    });
    removeFile();
    return;
  }
  if (!file.name.endsWith('.json') && file.type !== 'application/json') {
    window.DWMToast.error('Only JSON files are allowed.', {
      title: 'Import Data'
    });
    removeFile();
    return;
  }
  $('#dwm-file-input-wrapper').hide();
  $('#dwm-file-selected').show();
  $('#dwm-file-name').text(file.name);
  $('#dwm-file-size').text('(' + formatFileSize(file.size) + ')');
  $('#dwm-import-actions').show();
  $('#dwm-import-button').prop('disabled', false);
}
function removeFile() {
  selectedFile = null;
  $('#dwm-import-file').val('');
  $('#dwm-file-input-wrapper').show();
  $('#dwm-file-selected').hide();
  $('#dwm-file-name').text('');
  $('#dwm-file-size').text('');
  $('#dwm-import-actions').hide();
  $('#dwm-import-button').prop('disabled', true);
}
function importData() {
  const $btn = $('#dwm-import-button');
  const file = selectedFile;
  if (!file) {
    window.DWMToast.error('Please select a file to import.', {
      title: 'Import Data'
    });
    return;
  }
  (0,_dialog_js__WEBPACK_IMPORTED_MODULE_0__.dwmConfirm)({
    title: 'Import Data',
    message: 'Are you sure you want to import this data? This will replace all existing data.',
    icon: 'upload',
    confirmText: 'Import',
    onConfirm() {
      showLoading($btn);
      const formData = new FormData();
      formData.append('action', 'dwm_import');
      formData.append('nonce', dwmAdminVars.nonce);
      formData.append('file', file);
      $.ajax({
        url: dwmAdminVars.ajaxUrl,
        type: 'POST',
        data: formData,
        processData: false,
        contentType: false,
        success: function (response) {
          hideLoading($btn);
          if (response.success) {
            window.DWMToast.success(response.data.message || 'Data imported successfully.', {
              title: 'Import Data'
            });
            removeFile();
          } else {
            window.DWMToast.error(response.data && response.data.message || 'Import failed.', {
              title: 'Import Data'
            });
          }
        },
        error: function () {
          hideLoading($btn);
          window.DWMToast.error('Import request failed. Please try again.', {
            title: 'Import Data'
          });
        }
      });
    }
  });
}
function formatFileSize(bytes) {
  if (bytes < 1024) return bytes + ' B';
  if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(1) + ' KB';
  return (bytes / (1024 * 1024)).toFixed(1) + ' MB';
}

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Check if module exists (development only)
/******/ 		if (__webpack_modules__[moduleId] === undefined) {
/******/ 			var e = new Error("Cannot find module '" + moduleId + "'");
/******/ 			e.code = 'MODULE_NOT_FOUND';
/******/ 			throw e;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	!function() {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = function(exports, definition) {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	!function() {
/******/ 		__webpack_require__.o = function(obj, prop) { return Object.prototype.hasOwnProperty.call(obj, prop); }
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	!function() {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = function(exports) {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	}();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other modules in the chunk.
!function() {
/*!***************************************!*\
  !*** ./assets/js/components/tools.js ***!
  \***************************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _modules_partials_export_import_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../modules/partials/export-import.js */ "./assets/js/modules/partials/export-import.js");
/* harmony import */ var _modules_modals_reset_data_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../modules/modals/reset-data.js */ "./assets/js/modules/modals/reset-data.js");
/* harmony import */ var _modules_partials_demo_data_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../modules/partials/demo-data.js */ "./assets/js/modules/partials/demo-data.js");
/* harmony import */ var _modules_partials_cache_tools_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../modules/partials/cache-tools.js */ "./assets/js/modules/partials/cache-tools.js");
/**
 * Dashboard Widget Manager - Tools Page
 *
 * Entry point for the Tools admin page.
 * Imports tools dependencies and initializes tools interactions.
 *
 * Output: /assets/minimized/js/tools.min.js
 *
 * @package Dashboard_Widget_Manager
 * @since 1.0.0
 */





jQuery(document).ready(function () {
  (0,_modules_partials_export_import_js__WEBPACK_IMPORTED_MODULE_0__.initExportImport)();
  (0,_modules_modals_reset_data_js__WEBPACK_IMPORTED_MODULE_1__.initResetModal)();
  (0,_modules_partials_demo_data_js__WEBPACK_IMPORTED_MODULE_2__.initDemoData)();
  (0,_modules_partials_cache_tools_js__WEBPACK_IMPORTED_MODULE_3__.initCacheTools)();
});
}();
/******/ })()
;