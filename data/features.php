<?php
/**
 * Dashboard Widget Manager - Features Data
 *
 * Feature and integration registry data for the DWM features modal.
 *
 * @package Dashboard_Widget_Manager
 * @since 1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

return [

	'categories' => [
		__( 'Widget Management', 'dashboard-widget-manager' )  => [ 'icon' => 'layout', 'order' => 1, 'description' => __( 'Create, configure, and manage custom dashboard widgets with full control over ordering and visibility.', 'dashboard-widget-manager' ) ],
		__( 'Query Engine', 'dashboard-widget-manager' )        => [ 'icon' => 'database', 'order' => 2, 'description' => __( 'Write and execute SQL SELECT queries with built-in variable support, validation, and results preview.', 'dashboard-widget-manager' ) ],
		__( 'Performance', 'dashboard-widget-manager' ) => [ 'icon' => 'admin-settings', 'order' => 3, 'description' => __( 'Configure caching, execution limits, and query logging on a per-widget basis for fine-grained control.', 'dashboard-widget-manager' ) ],
		__( 'Template System', 'dashboard-widget-manager' )     => [ 'icon' => 'editor-code', 'order' => 4, 'description' => __( 'Build dynamic widget output using HTML/PHP templates with query result variable interpolation.', 'dashboard-widget-manager' ) ],
		__( 'Styles & Scripts', 'dashboard-widget-manager' )    => [ 'icon' => 'art', 'order' => 5, 'description' => __( 'Add per-widget custom CSS and JavaScript that is scoped and loaded only when the widget is displayed.', 'dashboard-widget-manager' ) ],
		__( 'Security', 'dashboard-widget-manager' )            => [ 'icon' => 'shield', 'order' => 6, 'description' => __( 'Built-in protections to keep your WordPress database safe when running custom SQL queries.', 'dashboard-widget-manager' ) ],
		__( 'Settings', 'dashboard-widget-manager' )            => [ 'icon' => 'admin-settings', 'order' => 7, 'description' => __( 'Configure security, export/import data, demo content, and reset options for the plugin.', 'dashboard-widget-manager' ) ],
	],

	'all_features' => [
		__( 'Widget Management', 'dashboard-widget-manager' ) => [
			[
				'id'           => 'create-widgets',
				'icon'         => '🧩',
				'title'        => __( 'Create Custom Widgets', 'dashboard-widget-manager' ),
				'description'  => __( 'Build unlimited custom dashboard widgets powered by SQL queries, templates, and custom code.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Unlimited widgets', 'dashboard-widget-manager' ),
					__( 'Named & described widgets', 'dashboard-widget-manager' ),
					__( 'Enable/disable per widget', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Each widget is a standalone unit with its own SQL query, HTML/PHP template, CSS, and JavaScript. You can create as many widgets as your dashboard needs — from a simple post count to a complex multi-table join showing revenue or user activity. Widgets are stored in a custom database table and rendered as standard WordPress dashboard meta boxes, so they integrate seamlessly with the native drag-to-reorder and hide/show controls WordPress already provides.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Navigate to Dashboard Widget Manager → Widget Manager and click "New Widget".', 'dashboard-widget-manager' ),
					__( 'Give the widget a name and an optional description to keep your list organized.', 'dashboard-widget-manager' ),
					__( 'Write a SELECT query in the Query tab, then build your display in the Template tab.', 'dashboard-widget-manager' ),
					__( 'Save the widget — it will appear on the WordPress dashboard immediately for admin users.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-create-widgets',
			],
			[
				'id'           => 'display-order',
				'icon'         => '🔢',
				'title'        => __( 'Display Order Control', 'dashboard-widget-manager' ),
				'description'  => __( 'Set a numeric display order for each widget and drag-to-reorder in the widget list.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Numeric order field', 'dashboard-widget-manager' ),
					__( 'Drag-and-drop sorting', 'dashboard-widget-manager' ),
					__( 'Instant reordering', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Every widget has an "Order" field that controls its position in the Widget Manager list. Lower numbers appear first. You can also drag rows up and down in the widget list to reorder them without manually editing each number — the order values update automatically via AJAX so there is nothing to save separately. This order is independent of the WordPress dashboard\'s own meta box order, which each user controls themselves via drag-and-drop on the dashboard screen.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Open any widget in the editor and set a numeric value in the "Order" field (e.g. 1, 2, 10, 20).', 'dashboard-widget-manager' ),
					__( 'Alternatively, drag the handle on the far left of any row in the widget list to reorder widgets visually.', 'dashboard-widget-manager' ),
					__( 'The order you set here controls which widget appears first in the manager — not the dashboard meta box order (that is per-user).', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-display-order',
			],
			[
				'id'           => 'widget-toggle',
				'icon'         => '🔄',
				'title'        => __( 'Widget Toggle', 'dashboard-widget-manager' ),
				'description'  => __( 'Instantly enable or disable any widget directly from the WordPress dashboard.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'One-click toggle', 'dashboard-widget-manager' ),
					__( 'Persistent state', 'dashboard-widget-manager' ),
					__( 'Safe disable without delete', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'The toggle switch on every widget row lets you turn a widget on or off without deleting it. A disabled widget is hidden from the WordPress dashboard immediately but all its configuration — query, template, CSS, JS — is preserved. This is useful for seasonal widgets, widgets under development, or widgets you want to pause temporarily without losing your work. The toggle state is saved via AJAX and takes effect without a page reload.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'In the Widget Manager list, find the toggle switch in the "Status" column next to any widget.', 'dashboard-widget-manager' ),
					__( 'Click the toggle to disable it — the switch turns grey and the widget disappears from the WordPress dashboard.', 'dashboard-widget-manager' ),
					__( 'Click it again to re-enable. No other changes are needed — the widget reappears on the dashboard instantly.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-widget-toggle',
			],
			[
				'id'           => 'widget-roles',
				'icon'         => '👥',
				'title'        => __( 'Role-Based Visibility', 'dashboard-widget-manager' ),
				'description'  => __( 'Restrict widgets to specific user roles so only authorized users see sensitive data.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Per-role visibility rules', 'dashboard-widget-manager' ),
					__( 'Multi-role support', 'dashboard-widget-manager' ),
					__( 'Admin override', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'By default every widget is visible to all dashboard users with the manage_options capability. Role-Based Visibility lets you narrow that down so a widget only appears for editors, shop managers, or any custom role you define. This is essential for multi-author sites or agencies where different users need different data views without exposing sensitive query results to everyone.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Open a widget and go to the Visibility tab (Pro feature).', 'dashboard-widget-manager' ),
					__( 'Select one or more roles that should see this widget.', 'dashboard-widget-manager' ),
					__( 'Save — users not in the selected roles will no longer see the widget on their dashboard.', 'dashboard-widget-manager' ),
					__( 'Administrators always see all widgets regardless of role rules.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'pro',
				'implemented'  => false,
				'docs_page'    => 'feature-widget-roles',
			],
		],

		__( 'Query Engine', 'dashboard-widget-manager' ) => [
			[
				'id'           => 'sql-queries',
				'icon'         => '🗄️',
				'title'        => __( 'SQL Query Editor', 'dashboard-widget-manager' ),
				'description'  => __( 'Write custom SELECT queries directly against your WordPress database using a syntax-highlighted editor.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Syntax-highlighted textarea', 'dashboard-widget-manager' ),
					__( 'Character & line counter', 'dashboard-widget-manager' ),
					__( 'JOINs, WHERE, ORDER BY, LIMIT', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'The query editor accepts any valid SQL SELECT statement that runs against your WordPress database. You can use JOINs, subqueries, GROUP BY, HAVING, ORDER BY, and LIMIT just as you would in any SQL tool. The editor provides syntax highlighting and tracks character and line counts to help you manage complex queries. All queries run through $wpdb->get_results() using prepared statements where variables are involved, so your database interactions are handled securely by WordPress core.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Open or create a widget and click the "Query" tab.', 'dashboard-widget-manager' ),
					__( 'Write your SELECT statement. Use {{table_prefix}} to reference tables portably across different WP installs.', 'dashboard-widget-manager' ),
					__( 'Click "Validate Query" to check syntax before saving.', 'dashboard-widget-manager' ),
					__( 'Click "Preview Results" to see live data from your database before committing the widget.', 'dashboard-widget-manager' ),
					__( 'Save the widget — the query runs every time the dashboard is loaded (or from cache if enabled).', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-sql-queries',
			],
			[
				'id'           => 'query-variables',
				'icon'         => '🔧',
				'title'        => __( 'Query Variables', 'dashboard-widget-manager' ),
				'description'  => __( 'Use built-in template variables in your SQL queries to reference dynamic values safely.', 'dashboard-widget-manager' ),
				'features'     => [
					__( '{{current_user_id}}', 'dashboard-widget-manager' ),
					__( '{{table_prefix}}, {{posts}}, {{users}}', 'dashboard-widget-manager' ),
					__( '{{site_url}}', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Query variables let you write a single query that adapts to context. Instead of hardcoding your table prefix or user ID, you use a {{variable}} placeholder that Dashboard Widget Manager replaces with the real value at runtime. All variable substitutions go through $wpdb->prepare() so there is no risk of injection. Common use cases include filtering results to the currently logged-in user, referencing tables portably across staging and production environments, or building user-specific activity widgets.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'In your SQL query, type {{variable_name}} where you want a dynamic value substituted.', 'dashboard-widget-manager' ),
					__( 'Use {{current_user_id}} to filter rows to the currently logged-in user (e.g. WHERE user_id = {{current_user_id}}).', 'dashboard-widget-manager' ),
					__( 'Use {{table_prefix}} to reference tables portably: {{table_prefix}}posts, {{table_prefix}}users, etc.', 'dashboard-widget-manager' ),
					__( 'Use {{posts}}, {{users}}, {{options}}, {{postmeta}}, {{usermeta}} as shorthand for those common tables.', 'dashboard-widget-manager' ),
					__( 'Use {{site_url}} to reference your site\'s base URL in queries that store URLs.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-query-variables',
			],
			[
				'id'           => 'query-validation',
				'icon'         => '✅',
				'title'        => __( 'Query Validation', 'dashboard-widget-manager' ),
				'description'  => __( 'Validate your SQL query before saving to catch syntax errors and ensure it is a safe SELECT statement.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'SELECT-only enforcement', 'dashboard-widget-manager' ),
					__( 'Syntax error detection', 'dashboard-widget-manager' ),
					__( 'Inline validation result', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Clicking "Validate Query" sends your query to the server where it is parsed and checked without actually executing it against your data. The validator rejects any query that is not a SELECT statement, blocks dangerous keywords regardless of position (INSERT, UPDATE, DELETE, DROP, TRUNCATE, ALTER, EXEC), and runs a dry-run EXPLAIN to surface syntax errors before you save. The result — pass or a specific error message — is shown inline below the editor so you can fix issues immediately.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Write your query in the Query tab editor, then click the "Validate Query" button.', 'dashboard-widget-manager' ),
					__( 'A green success notice means the query is syntactically valid and passes all security checks.', 'dashboard-widget-manager' ),
					__( 'A red error notice shows the specific problem — fix it and validate again before saving.', 'dashboard-widget-manager' ),
					__( 'Validation also runs automatically when you click Save, blocking the save if the query fails.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-query-validation',
			],
			[
				'id'           => 'results-preview',
				'icon'         => '🔍',
				'title'        => __( 'Results Preview', 'dashboard-widget-manager' ),
				'description'  => __( 'Preview live query results in a paginated, searchable table modal before saving the widget.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Live data preview', 'dashboard-widget-manager' ),
					__( 'Searchable results', 'dashboard-widget-manager' ),
					__( 'Pagination support', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Results Preview executes your query against your actual database and displays the rows in a clean table inside a modal — without you having to save and reload the dashboard widget first. This dramatically speeds up query iteration: you can see exactly what columns your query returns, check that JOINs are resolving correctly, and verify row counts before committing. The preview table is paginated (25 rows per page by default) and includes a search input to filter rows client-side, which is helpful when previewing large result sets.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'In the Query tab, click "Preview Results" after writing your query.', 'dashboard-widget-manager' ),
					__( 'The modal opens with a table of your live query results, showing all returned columns.', 'dashboard-widget-manager' ),
					__( 'Use the search box to filter rows by any value, or use the pagination controls to navigate large result sets.', 'dashboard-widget-manager' ),
					__( 'Close the modal and adjust your query if needed, then preview again — there is no limit on how many times you can preview.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-results-preview',
			],
		],

		__( 'Performance', 'dashboard-widget-manager' ) => [
			[
				'id'           => 'caching',
				'icon'         => '⚡',
				'title'        => __( 'Caching', 'dashboard-widget-manager' ),
				'description'  => __( 'Enable query result caching on a per-widget basis with a configurable TTL to boost performance.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Enable/disable caching', 'dashboard-widget-manager' ),
					__( 'Configurable TTL (0–3600s)', 'dashboard-widget-manager' ),
					__( 'Transient-based cache', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Dashboard Widget Manager uses WordPress transients to cache query results per widget. When caching is enabled, the first dashboard load executes the query and stores the result. All subsequent loads within the TTL window read from the transient — no database query is run. This is especially valuable for complex queries with JOINs or aggregations that run on every admin page load. Each widget has its own independent cache key so enabling caching on one widget does not affect others. When the TTL expires the cache is refreshed automatically on the next load.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Open a widget and go to the Performance tab.', 'dashboard-widget-manager' ),
					__( 'Toggle "Enable Caching" on.', 'dashboard-widget-manager' ),
					__( 'Set a TTL in seconds (e.g. 300 for 5 minutes, 3600 for 1 hour). A value of 0 disables caching.', 'dashboard-widget-manager' ),
					__( 'Save the widget. The next dashboard load will prime the cache; subsequent loads within the TTL will not run the query.', 'dashboard-widget-manager' ),
					__( 'To force a cache refresh, temporarily set TTL to 0, save, load the dashboard, then re-enable with your desired TTL.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-caching',
			],
			[
				'id'           => 'execution-time',
				'icon'         => '⏱️',
				'title'        => __( 'Execution Time Limit', 'dashboard-widget-manager' ),
				'description'  => __( 'Set a maximum execution time so slow queries are automatically aborted.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Configurable limit (1–60s)', 'dashboard-widget-manager' ),
					__( 'Automatic query abort', 'dashboard-widget-manager' ),
					__( 'Protects server resources', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'An unbounded query on a large table can lock MySQL for seconds, making your entire WordPress admin feel sluggish. The execution time limit sets a per-widget maximum in seconds; if the query takes longer than that threshold it is aborted and the widget shows a timeout error instead of hanging the page. This is a safety net for queries in development or for datasets that unexpectedly grow large. Pair it with caching so slow queries only run at cache-miss time rather than on every page load.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Open a widget and go to the Performance tab.', 'dashboard-widget-manager' ),
					__( 'Set the "Max Execution Time" field to a value in seconds (1–60). The default is 5.', 'dashboard-widget-manager' ),
					__( 'Save. If the query exceeds this limit the widget will display a timeout notice on the dashboard.', 'dashboard-widget-manager' ),
					__( 'If you see timeout errors, optimize your query with proper indexes or increase the limit. Enabling caching minimizes how often the limit is tested.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-execution-time',
			],
			[
				'id'           => 'query-logging',
				'icon'         => '📋',
				'title'        => __( 'Query Logging', 'dashboard-widget-manager' ),
				'description'  => __( 'Enable debug logging for individual widgets to record execution times in the WordPress debug log.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Enable/disable per widget', 'dashboard-widget-manager' ),
					__( 'Execution time recording', 'dashboard-widget-manager' ),
					__( 'Requires WP_DEBUG_LOG', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Query Logging writes a timestamped entry to your WordPress debug log every time a widget\'s query runs, recording the widget name, query execution time in milliseconds, and whether the result came from cache. This is a development and debugging tool — use it temporarily while profiling queries, then disable it to avoid filling up your debug log. Since each log entry includes execution time, you can quickly spot which widgets are the slowest and decide whether to add indexes or enable caching.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Make sure WP_DEBUG and WP_DEBUG_LOG are both set to true in your wp-config.php.', 'dashboard-widget-manager' ),
					__( 'Open a widget and go to the Performance tab, then toggle "Enable Query Logging" on.', 'dashboard-widget-manager' ),
					__( 'Save and load the WordPress dashboard to trigger the query.', 'dashboard-widget-manager' ),
					__( 'Check wp-content/debug.log for lines tagged [DWM] to see execution times.', 'dashboard-widget-manager' ),
					__( 'Disable logging on production once you have finished profiling.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-query-logging',
			],
		],

		__( 'Template System', 'dashboard-widget-manager' ) => [
			[
				'id'           => 'html-php-templates',
				'icon'         => '📄',
				'title'        => __( 'HTML/PHP Templates', 'dashboard-widget-manager' ),
				'description'  => __( 'Write HTML and PHP templates for each widget with full access to query results via $query_results.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Full PHP support', 'dashboard-widget-manager' ),
					__( '$query_results array access', 'dashboard-widget-manager' ),
					__( 'Syntax-highlighted editor', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'The template editor accepts any combination of HTML and PHP. When the widget renders, the template is evaluated in a sandboxed context where $query_results holds the array of rows returned by your query — each row is an object with properties matching your SELECT columns. You have the full power of PHP to loop over results, format numbers, conditionally show or hide content, build tables, render charts via inline JS, or output anything the WordPress dashboard supports. The template editor provides syntax highlighting for both HTML and PHP to make editing comfortable.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Open a widget and click the "Template" tab.', 'dashboard-widget-manager' ),
					__( 'Write HTML/PHP in the editor. Use $query_results to access your query data — it is always an array of stdClass objects.', 'dashboard-widget-manager' ),
					__( 'Example: foreach ($query_results as $row) { echo esc_html($row->post_title); } to list post titles.', 'dashboard-widget-manager' ),
					__( 'Use {{variable_name}} shorthand for simple column output (auto-escapes HTML).', 'dashboard-widget-manager' ),
					__( 'Save and view the WordPress dashboard to see your template rendered inside the widget meta box.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-html-php-templates',
			],
			[
				'id'           => 'variable-interpolation',
				'icon'         => '🔀',
				'title'        => __( 'Variable Interpolation', 'dashboard-widget-manager' ),
				'description'  => __( 'Use {{variable_name}} syntax to output query column values directly in your template.', 'dashboard-widget-manager' ),
				'features'     => [
					__( '{{column_name}} shorthand', 'dashboard-widget-manager' ),
					__( 'Auto-escaping helpers', 'dashboard-widget-manager' ),
					__( '{{esc_html:var}}, {{esc_url:var}}', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Variable interpolation is a simpler alternative to writing PHP loops for straightforward templates. When your query returns a single row (or you want to output the first row\'s value), you can write {{column_name}} anywhere in your template HTML and Dashboard Widget Manager replaces it with the value from that column, HTML-escaped by default. For multi-row output you still use PHP loops, but for stat-style widgets that show a single number or name, interpolation keeps the template clean and readable. Escape variants {{esc_html:var}} and {{esc_url:var}} give you explicit control over output encoding.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'In your template, write {{column_name}} where you want a query result value to appear.', 'dashboard-widget-manager' ),
					__( 'The variable name must exactly match a column returned by your SELECT query (case-sensitive).', 'dashboard-widget-manager' ),
					__( 'Values are HTML-escaped by default. For URLs, use {{esc_url:column_name}} to apply URL escaping instead.', 'dashboard-widget-manager' ),
					__( 'Example: SELECT COUNT(*) as total_posts FROM wp_posts — then use <strong>{{total_posts}}</strong> in your template to display the count.', 'dashboard-widget-manager' ),
					__( 'For multi-row queries, use PHP loops ($query_results) instead of interpolation since {{}} only targets the first row.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-variable-interpolation',
			],
		],

		__( 'Styles & Scripts', 'dashboard-widget-manager' ) => [
			[
				'id'           => 'custom-css',
				'icon'         => '🎨',
				'title'        => __( 'Per-Widget Custom CSS', 'dashboard-widget-manager' ),
				'description'  => __( 'Add custom CSS for each widget. Styles are automatically scoped so they only affect that widget.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Scoped to widget container', 'dashboard-widget-manager' ),
					__( 'Syntax-highlighted editor', 'dashboard-widget-manager' ),
					__( 'No wrapper selectors needed', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'When you add CSS to a widget, Dashboard Widget Manager wraps every rule with the widget\'s unique container selector automatically. This means you can write plain selectors like table, th, .my-class without worrying about leaking styles to the rest of the WordPress admin. Each widget\'s CSS is injected into a <style> tag inside its meta box so it only applies within that widget\'s rendered output. This is ideal for styled tables, custom stat cards, color-coded status indicators, or any visual formatting that goes beyond default WordPress dashboard styling.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Open a widget and click the "Styles" tab.', 'dashboard-widget-manager' ),
					__( 'Write your CSS rules in the editor. You do not need to add a wrapper selector — styles are automatically scoped.', 'dashboard-widget-manager' ),
					__( 'Example: table { width: 100%; border-collapse: collapse; } th { background: #f0f0f0; }', 'dashboard-widget-manager' ),
					__( 'Save and reload the dashboard to see the styles applied inside your widget.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-custom-css',
			],
			[
				'id'           => 'custom-js',
				'icon'         => '⚙️',
				'title'        => __( 'Per-Widget Custom JavaScript', 'dashboard-widget-manager' ),
				'description'  => __( 'Add custom JavaScript to each widget. Scripts are loaded when the widget is displayed and jQuery is available.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'jQuery available', 'dashboard-widget-manager' ),
					__( 'Loaded with widget', 'dashboard-widget-manager' ),
					__( 'Syntax-highlighted editor', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Per-widget JavaScript runs in the context of the WordPress admin dashboard where jQuery is always available. The script is injected immediately after the widget\'s HTML so you can safely query the DOM for elements inside the widget. Use this to initialize small charts (Chart.js, ApexCharts), add click handlers for expandable rows, auto-refresh widget content on a timer, or integrate with any third-party JS library you enqueue separately. Each widget\'s script is isolated in a function wrapper to prevent variable collisions between multiple widgets on the same dashboard.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Open a widget and click the "Scripts" tab.', 'dashboard-widget-manager' ),
					__( 'Write your JavaScript in the editor. jQuery (as $) is available and the widget DOM is already rendered above the script.', 'dashboard-widget-manager' ),
					__( 'Target elements inside the widget using the unique widget container ID (shown above the editor).', 'dashboard-widget-manager' ),
					__( 'Example: $(\'.dwm-widget-123 table tr\').on(\'click\', function() { $(this).toggleClass(\'selected\'); });', 'dashboard-widget-manager' ),
					__( 'Save and reload the dashboard. Your script runs once per page load when the widget meta box renders.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-custom-js',
			],
		],

		__( 'Security', 'dashboard-widget-manager' ) => [
			[
				'id'           => 'select-only',
				'icon'         => '🛡️',
				'title'        => __( 'SELECT-Only Queries', 'dashboard-widget-manager' ),
				'description'  => __( 'All SQL queries are strictly validated to ensure only SELECT statements can be executed — no data modification.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'INSERT/UPDATE/DELETE blocked', 'dashboard-widget-manager' ),
					__( 'DROP/TRUNCATE blocked', 'dashboard-widget-manager' ),
					__( 'Server-side enforcement', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Dashboard Widget Manager enforces SELECT-only queries at two layers. First, during save and validation the query is statically analyzed to reject any statement that is not a SELECT or that contains write-operation keywords (INSERT, UPDATE, DELETE, DROP, TRUNCATE, ALTER, EXEC, GRANT, REVOKE) anywhere in the query, including inside subqueries or comments. Second, at runtime $wpdb is configured to block non-SELECT operations for widget queries. This means even if a cleverly crafted query somehow passed the first layer, the database layer would reject it. The goal is to make widgets a safe read-only window into your data.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'No configuration is required — SELECT-only enforcement is always active and cannot be disabled.', 'dashboard-widget-manager' ),
					__( 'If your query is rejected by the validator even though it looks like a SELECT, check for subqueries or CTEs that contain write keywords.', 'dashboard-widget-manager' ),
					__( 'For displaying data that requires writes (e.g. logging a view count), handle the write outside the widget using a WP action hook instead.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-select-only',
			],
			[
				'id'           => 'table-allowlist',
				'icon'         => '📋',
				'title'        => __( 'Table Allowlist', 'dashboard-widget-manager' ),
				'description'  => __( 'Restrict queries to a specific list of allowed database tables for an extra layer of access control.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Configurable table list', 'dashboard-widget-manager' ),
					__( 'Blocks unauthorized tables', 'dashboard-widget-manager' ),
					__( 'Settings page control', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'The Table Allowlist is configured on the Settings page and lists every database table that widget queries are permitted to reference. Any query that references a table not on the list is blocked before execution. This is particularly important in shared or multi-author environments where you want to ensure widget authors cannot query sensitive tables like wp_users, wp_usermeta, or custom tables containing payment data. By default all WordPress core tables are included in the allowlist; you can deselect any you want to restrict. Tables from plugins you install appear in the list automatically once they exist in your database.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Go to Dashboard Widget Manager → Settings → Security.', 'dashboard-widget-manager' ),
					__( 'Review the table list — all tables in your database are shown with checkboxes.', 'dashboard-widget-manager' ),
					__( 'Uncheck any tables you do not want widget queries to be able to reference.', 'dashboard-widget-manager' ),
					__( 'Click Save. Queries that reference a disallowed table will now return an error instead of executing.', 'dashboard-widget-manager' ),
					__( 'Use "Select All" or "Deselect All" as a starting point, then fine-tune per table.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-table-allowlist',
			],
			[
				'id'           => 'capability-check',
				'icon'         => '🔐',
				'title'        => __( 'Capability Checks', 'dashboard-widget-manager' ),
				'description'  => __( 'All admin operations require the manage_options capability, ensuring only administrators can create or edit widgets.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'manage_options required', 'dashboard-widget-manager' ),
					__( 'Nonce verification on all AJAX', 'dashboard-widget-manager' ),
					__( 'WordPress-standard security', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Every admin action in Dashboard Widget Manager — creating widgets, saving queries, toggling status, running imports, resetting data — is gated behind a current_user_can(\'manage_options\') check. This is the same capability required to manage WordPress options and is held only by administrators by default. In addition, every AJAX request includes a nonce that is verified server-side before any operation proceeds. If either check fails, the request is rejected with a 403 error. This prevents editors, authors, or any non-admin user from interacting with widget data even if they discover the AJAX endpoints.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'No configuration is required — capability checks are always active and apply to all plugin operations.', 'dashboard-widget-manager' ),
					__( 'Only users with the Administrator role (or a custom role granted manage_options) can access the Widget Manager menu pages.', 'dashboard-widget-manager' ),
					__( 'Widget display on the dashboard follows standard WordPress meta box visibility — widgets are only rendered for users who can access the dashboard.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'feature-capability-check',
			],
		],

		__( 'Settings', 'dashboard-widget-manager' ) => [
			[
				'id'           => 'security-settings',
				'icon'         => '🔒',
				'title'        => __( 'Security Settings', 'dashboard-widget-manager' ),
				'description'  => __( 'Configure which database tables are allowed for use in widget SQL queries.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Table allowlist configuration', 'dashboard-widget-manager' ),
					__( 'Select All / Deselect All controls', 'dashboard-widget-manager' ),
					__( 'Per-table checkbox selection', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'The Security section of the Settings page is the control panel for the Table Allowlist feature. It lists every table currently in your WordPress database, grouped by plugin or origin where possible. Use the checkboxes to mark which tables widget queries may reference. Changes take effect immediately on save — any widget whose query references a newly disallowed table will show an error on the dashboard until the query is updated to use only allowed tables.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Navigate to Dashboard Widget Manager → Settings → Security section.', 'dashboard-widget-manager' ),
					__( 'All database tables are listed with checkboxes. Checked = allowed in queries, unchecked = blocked.', 'dashboard-widget-manager' ),
					__( 'Use "Select All" to allow everything (default) or "Deselect All" then check only the tables you want exposed.', 'dashboard-widget-manager' ),
					__( 'Click Save Settings to apply. The table list refreshes automatically when new tables are added by plugins.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'settings-security',
			],
			[
				'id'           => 'export-data',
				'icon'         => '📤',
				'title'        => __( 'Export Data', 'dashboard-widget-manager' ),
				'description'  => __( 'Download all widget configurations and settings as a JSON file for backup or migration.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Export widgets and/or settings', 'dashboard-widget-manager' ),
					__( 'Download as JSON file', 'dashboard-widget-manager' ),
					__( 'Preview data before export', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'The export system captures your entire widget library and plugin settings into a single JSON file you can download, store, or import to another site. You can choose to export widgets only, settings only, or both. The "View Data" modal lets you preview the full JSON before downloading so you can verify exactly what will be included. Exports are timestamped with the plugin version, WordPress version, and site URL, which helps identify files when managing backups across multiple environments.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Go to Dashboard Widget Manager → Settings → Import / Export section.', 'dashboard-widget-manager' ),
					__( 'Click "Export" to open the export options modal.', 'dashboard-widget-manager' ),
					__( 'Choose whether to include Widgets, Settings, or both using the toggle switches.', 'dashboard-widget-manager' ),
					__( 'Optionally click "View Data" first to preview the JSON that will be exported.', 'dashboard-widget-manager' ),
					__( 'Click "Download JSON" to save the file to your computer.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'settings-export',
			],
			[
				'id'           => 'import-data',
				'icon'         => '📥',
				'title'        => __( 'Import Data', 'dashboard-widget-manager' ),
				'description'  => __( 'Import widget configurations and settings from a previously exported JSON file.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Drag-and-drop file input', 'dashboard-widget-manager' ),
					__( 'JSON preview before importing', 'dashboard-widget-manager' ),
					__( 'Replace all data mode', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'Import accepts any JSON file exported by Dashboard Widget Manager. The file can be dragged directly onto the drop zone or selected via the file browser. Before the import runs, a preview of the JSON content appears so you can confirm the file looks correct. The current import mode replaces all existing data — all current widgets and settings are overwritten with the contents of the file. Use the Export feature to back up your current data before importing if you need to preserve it.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Go to Dashboard Widget Manager → Settings → Import / Export section.', 'dashboard-widget-manager' ),
					__( 'Drag a .json export file onto the drop zone, or click the zone to browse for the file.', 'dashboard-widget-manager' ),
					__( 'Review the JSON preview that appears to confirm the file content.', 'dashboard-widget-manager' ),
					__( 'Select import options (Widgets, Settings, or both) using the toggle switches.', 'dashboard-widget-manager' ),
					__( 'Click "Import" and confirm the warning dialog — existing data will be replaced.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'settings-import',
			],
			[
				'id'           => 'demo-data',
				'icon'         => '🎭',
				'title'        => __( 'Demo Data', 'dashboard-widget-manager' ),
				'description'  => __( 'Import pre-built demo widgets to explore plugin capabilities without writing queries from scratch.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'One-click demo widget import', 'dashboard-widget-manager' ),
					__( 'Delete demo widgets when done', 'dashboard-widget-manager' ),
					__( 'Export demo data as JSON', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'The demo dataset ships a curated collection of example widgets that showcase different features of the plugin — stat-style widgets, table layouts, multi-join queries, and template examples. Importing demo data is non-destructive: it adds the demo widgets alongside any widgets you already have rather than replacing them. Once you have explored the examples you can delete the demo widgets in a single click using the "Delete Demo Data" button. You can also export the demo data as JSON and use it as a starting point or reference when building your own widgets.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Go to Dashboard Widget Manager → Settings → Demo Data section.', 'dashboard-widget-manager' ),
					__( 'Click "Import Demo Data" and confirm the dialog. Demo widgets are added to your widget library.', 'dashboard-widget-manager' ),
					__( 'Visit the WordPress dashboard to see the demo widgets rendered in real time.', 'dashboard-widget-manager' ),
					__( 'When finished, click "Delete Demo Data" in the same section to remove all demo widgets at once.', 'dashboard-widget-manager' ),
					__( 'Optionally use "Export Demo Data" to download the demo JSON and use it as a template for your own widgets.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'settings-demo-data',
			],
			[
				'id'           => 'reset-data',
				'icon'         => '🗑️',
				'title'        => __( 'Reset Data', 'dashboard-widget-manager' ),
				'description'  => __( 'Permanently delete all widget configurations and/or reset plugin settings to defaults.', 'dashboard-widget-manager' ),
				'features'     => [
					__( 'Selective reset (widgets and/or settings)', 'dashboard-widget-manager' ),
					__( 'Export before reset option', 'dashboard-widget-manager' ),
					__( 'Two-step confirmation required', 'dashboard-widget-manager' ),
				],
				'details'      => __( 'The Reset Data tool gives you a clean slate when needed — useful when onboarding a site, cleaning up after testing, or starting over with a fresh configuration. You can choose to reset widgets only, settings only, or both. Resetting widgets deletes all widget records from the database; resetting settings returns all plugin configuration to factory defaults. Both actions are permanent and irreversible, which is why the tool requires two confirmations and prominently offers an "Export first" option so you always have a backup before proceeding.', 'dashboard-widget-manager' ),
				'instructions' => [
					__( 'Go to Dashboard Widget Manager → Settings → Reset Data section.', 'dashboard-widget-manager' ),
					__( 'Click "Reset Data" to open the confirmation modal.', 'dashboard-widget-manager' ),
					__( 'Use "Export First" in the modal to download a backup before proceeding.', 'dashboard-widget-manager' ),
					__( 'Check "Widgets" and/or "Settings" to select what to reset.', 'dashboard-widget-manager' ),
					__( 'Click the red "Reset" button and confirm the second warning dialog. The action cannot be undone.', 'dashboard-widget-manager' ),
				],
				'plan'         => 'free',
				'implemented'  => true,
				'docs_page'    => 'settings-reset',
			],
		],
	],

	'integrations' => [
		__( 'Monitoring', 'dashboard-widget-manager' ) => [
			[
				'id'          => 'new-relic',
				'icon'        => '📈',
				'title'       => __( 'New Relic', 'dashboard-widget-manager' ),
				'description' => __( 'Surface New Relic performance metrics directly in your WordPress dashboard.', 'dashboard-widget-manager' ),
				'features'    => [
					__( 'Widget-based metrics display', 'dashboard-widget-manager' ),
					__( 'Custom SQL against NR data', 'dashboard-widget-manager' ),
					__( 'Real-time performance view', 'dashboard-widget-manager' ),
				],
				'implemented' => false,
				'docs_page'   => 'integration-new-relic',
			],
			[
				'id'          => 'datadog',
				'icon'        => '🐕',
				'title'       => __( 'Datadog', 'dashboard-widget-manager' ),
				'description' => __( 'Embed Datadog metrics and alerts as custom dashboard widgets.', 'dashboard-widget-manager' ),
				'features'    => [
					__( 'API-driven data display', 'dashboard-widget-manager' ),
					__( 'Custom metric widgets', 'dashboard-widget-manager' ),
					__( 'Alert status overview', 'dashboard-widget-manager' ),
				],
				'implemented' => false,
				'docs_page'   => 'integration-datadog',
			],
		],
		__( 'Database Tools', 'dashboard-widget-manager' ) => [
			[
				'id'          => 'phpmyadmin',
				'icon'        => '🗄️',
				'title'       => __( 'phpMyAdmin', 'dashboard-widget-manager' ),
				'description' => __( 'Link dashboard widgets to phpMyAdmin query exports for advanced database exploration.', 'dashboard-widget-manager' ),
				'features'    => [
					__( 'Query export link', 'dashboard-widget-manager' ),
					__( 'Direct table navigation', 'dashboard-widget-manager' ),
					__( 'Read-only safe queries', 'dashboard-widget-manager' ),
				],
				'implemented' => false,
				'docs_page'   => 'integration-phpmyadmin',
			],
		],
	],

	'integrations_meta' => [
		'icon'        => 'admin-plugins',
		'label'       => __( 'Integrations', 'dashboard-widget-manager' ),
		'description' => __( 'Connect Dashboard Widget Manager with external tools and services to extend your dashboard capabilities.', 'dashboard-widget-manager' ),
	],

	'labels' => [
		'badge_pro'         => __( 'Pro', 'dashboard-widget-manager' ),
		'badge_free'        => __( 'Free', 'dashboard-widget-manager' ),
		'badge_coming_soon' => __( 'Coming Soon', 'dashboard-widget-manager' ),
		'badge_available'   => __( 'Available', 'dashboard-widget-manager' ),
	],
];
